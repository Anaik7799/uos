[package]
name = "c3i-planning-e2e"
version = "0.1.0"
edition = "2021"

[dependencies]
tungstenite = { version = "0.24", features = ["native-tls"] }
native-tls = "0.2"
serde_json = "1"
reqwest = { version = "0.12", features = ["blocking", "native-tls", "json"], default-features = false }
regex = "1"
