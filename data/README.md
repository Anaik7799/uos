# Data Subsystem (`data/`)

- **Ownership & Language Authority**: Declarative schemas and SQL.
- **Scope**: Canonical SQLite database schemas, ontological dictionaries, initial seed ledgers.
- **Invariants**: Immutable schema migrations. WAL/SHM live runtime files are never checked into version control.
