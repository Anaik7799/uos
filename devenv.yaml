# UOS devenv inputs (SC-TOOLCHAIN-INPROJECT-001, SC-NIX-DEVENV-001).
# Pinned through Determinate Nix / FlakeHub rather than a floating channel.
inputs:
  nixpkgs:
    url: https://flakehub.com/f/DeterminateSystems/nixpkgs-weekly/*.tar.gz
    flake: true
