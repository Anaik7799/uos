# ADR-062: uos_tui Swarm, Hive-Mind Message Board, Coordination Layer, Agent Communication Language and C3I Zenoh Infra

- **Status**: Proposed (built, executed, passed; live delivery proof acknowledged; awaiting two-key verification and tri-sovereign review)
- **Date**: 2026-09-07
- **Timestamp**: `20260907-0537-`
- **Deciders**: Claude Fable 5.1 (design authority, L0); runtime by the deterministic F´ manager (0 tokens/cycle)
- **Consulted**: C3I `agui/zenoh_bus`, `zenoh/*`, `gateway/matrix/bridge`, `substrate/beam_cache`, `ha/runbooks`; ZigVM ADR-004, codex-harness-mcp-zenoh-bridge, ooda-sa-plan control plane; the operator-shared C3I assessment
- **Governing Plan**: [`PLAN-UOS-TUI-SWARM-001`](file:///home/an/NAS-setup/uos/docs/plans/20260907-0440-uos-tui-swarm-15-agent-multilayer-plan.md)
- **Tailscale Web Link**: [`http://nas-1.tail55d152.ts.net:4100/zk/docs/zk/20260907-0537-adr-062-uos-tui-swarm-hive-mind-message-board-coordination-acl-and-zenoh-infra.md`](http://nas-1.tail55d152.ts.net:4100/zk/20260907-0537-adr-062-uos-tui-swarm-hive-mind-message-board-coordination-acl-and-zenoh-infra.md)
- **Live surfaces**: [Zenoh REST a2a plane](http://nas-1.tail55d152.ts.net:8080/c3i/a2a/**) · [shared state](http://nas-1.tail55d152.ts.net:8080/uos/tui/state/**) · [hive snapshot](http://nas-1.tail55d152.ts.net:8080/uos/tui/state/hive)
- **Fractal Coordinates**: `#fractal-l0` through `#fractal-l9`
- **Knowledge Tags**: `#zk-adr` `#uos-tui` `#swarm` `#hive-mind` `#zenoh` `#rocha-semiotics` `#cybernetics` `#km-triad` `#zero-muda` `#checklist-nav` `#tailscale-web` `#fprime-agents` `#stamp-stpa`
- **Transclusions**: `[[zk:20260906-2150-adr-061-uos-tui-gleam-library-textual-reference]]` `[[wiki:20260907-0537-uos-hive-mind-architecture-wiki]]` `[[wiki:20260906-2150-uos-fractal-textual-ontology-wiki]]` `[[zk:20260905-1801-moc-uos-unified-master]]` `[[wiki:20260905-1801-uos-zk-km-corpus-index]]`

---

## Context

ADR-061 delivered uos_tui v0.1 (116 tests). The operator then directed, in one accumulating prompt, a 15-agent swarm at the cheapest viable intelligence under a multilayer supervisor with full jj use; a shared, fully tracked message board over Zenoh with ETS as the live table; the C3I Zenoh infra started; a coordination and sync layer for distributed agents with hierarchical control so the security model holds; per-agent and global resource tracking; sharing of every key system aspect; a system-wide 17-aspect audit; an intelligent F´ managing agent; design only by Fable and runtime by the cheapest intelligence; a full agent communication language optimised for shared thinking with formal structure; fractal holonic techniques on every plane; Sanskrit mirrored with English; and the prompt, analysis and system details saved in the journal. The operator also shared a C3I assessment finding no durable inbox, no ack/retry/dead-letter/replay semantics and no live delivery proof.

## Decisions

1. **Swarm**: 11 Sonnet/Haiku slice workers in 11 jj sibling workspaces on base `vvrtrspv`, 4 Haiku group verifiers, pipeline per group, no retries, jidoka on red; integration by `jj squash --from <ws>@ --into @` (11 slices, 25 files, 0 conflicts). Result: 11/11 PASS first attempt, 172 tests added, 3,959 LOC, 2,591,767 subagent tokens, 324 s wall.
2. **Message board** (`uos_tui/board`): ETS ordered-set live table, append-only JSONL ledger (latest line per id wins), Zenoh REST transport on `c3i/a2a/{source}/{target}/{id}` (cepaf a2a plane, one key per message so storage keeps all). Envelope: id, W3C trace/span, µs ISO time, Lamport, sender/receiver with layer and model, kind, key, typed payload, semantics (ontology concepts, aspects, STPA control actions, muda, fractal layer), causality, per-sender SHA-256 chain, honest per-transport delivery. Inbox, Ack, retry with dead-letter after 5 attempts, replay, delivery state machine created→outboxed→published→acknowledged|dead.
3. **Coordination** (`uos_tui/coord`): hierarchical policy (L0..L3 kinds; Intent only upward; control keys L0/L1; **design kinds Plan/Dispatch/Integrate require a Fable-class sender**), fenced leases with epochs, task claims under a WIP limit, heartbeats with a dead-man's switch and retirement, Lamport clocks, reconcile against Zenoh storage (digest-verified pull, push of local-only), usage accounting with documented relative cost weights, shared-state publishing on `uos/tui/state/*`, OTP actor with child spec, and a live delivery proof.
4. **Zenoh infra**: `c3i-zenoh-router-1` (eclipse/zenoh, host network) from `ops/zenoh/20260907-0450-uos-zenoh-router-1.json5` derived from the C3I router config; REST on 8080 (8000 is the printer service); memory storages on `c3i/a2a/**`, `c3i/agui/events/**`, `uos/tui/**`; user systemd unit enabled. The running cockpit reconnected by itself.
5. **System-wide audit** (`uos_tui/system_audit`): 8 subjects × 17 aspects, fail-closed probes. Final: PASS 62, DECLARED 74, FAIL 0, admissible.
6. **F´ manager** (`uos_tui/manager`): Active component `UosManager` at base 0x2040 with its own dictionary; pure OODA `step` over board, leases, heartbeats, Zenoh and audit; acts only through authorized board messages (Rocha cut); speaks the ACL.
7. **Language** (`uos_tui/acl`): 12 FIPA-style performatives with formal pre/post conditions and a bounded conversation protocol; modal clauses K/B/I/O/G/∴ with conjunctions and ⇐ preconditions; vocabulary bound fail-closed to ontology, aspects, STPA and muda; Shannon entropy and semantic-density metrics; introspection in plain language; **Sanskrit-first canonical form (IAST) with Devanagari accepted and the English form beside every line**.
8. **Holarchy** (`uos_tui/holon`): 33 holons across 7 planes (control, structure, runtime, data, messaging, intelligence, language), validated acyclic and level-monotonic, addressed on `uos/holon/**`, each with a Sanskrit name.
9. **Hive mind**: one consolidated snapshot (`hive` command) published on `uos/tui/state/hive` with holarchy, board, KPIs, audit, language and Zenoh state.

10. **Agent cognition kernel** (`uos_tui/agent_runtime`): per-agent ETS memory (owner-write, L0/L1-read), F´ hierarchical lifecycle machine (Idle→Claimed→Working→Verifying→Done|Failed), bounded Rete-style forward chaining seeded from STPA/TPS constraints, Beta-Binomial success beliefs with cheapest-adequate model choice (deterministic Thompson draw), default-deny capability grants (Lean, Quint, STM, Zenoh, MAX/Mojo, Rete, Bayes, Memory) issued only by L0/L1 and tracked on the board, MAX access brokered as an upward Intent, and a controls report (operational, security, observability, formal) with live status.

## Ideas adopted from C3I and ZigVM (sourced by the Haiku explorer)
Adopted now: a2a topic schema (`agui/zenoh_bus.gleam`); bridge inbox/outbox queues (`gateway/matrix/bridge.gleam`); ETS shared cache pattern (`substrate/beam_cache.gleam`); persistent session lifecycle (ZigVM ADR-004); at-least-once with idempotent replay and fencing (codex-harness-mcp-zenoh-bridge); hierarchical leasing per OODA slice (ooda-sa-plan control plane). Deferred: CRDT version vectors (`crdt.gleam`), RFC 6902 patch deltas (`agui/state.gleam`), ETS→SQLite checkpoint (`checkpoint.gleam`), runbook recovery steps (`ha/runbooks.gleam`), 2oo3 voting for verifier consensus (`zenoh/safety.gleam`).

## Sovereign review and hardening (same session)
Antigravity (Gemini 3.8 Flash) returned **HOLD** with a correct P0: forged authority via the unauthenticated REST port and unkeyed digests. Adopted: HMAC-signed envelopes (key outside repo), policy + signature checks on absorb, protected ETS, claim-epoch fencing, real Beta sampler, honest SEC-8 status; live forgery probe now refused. Open: Hermes hook exercised against injection payloads (binary present), CHK-09 measurement, persistent Zenoh storage, native subscription. Codex Astra (gpt-6-astra) returned **HOLD** with eight P1 findings; all eight were closed in hardening round H2 (five Sonnet workers): injective canonical form `uos-board-canon/2`, per-sender signing keys, ledger-first transactional outbox with quarantine, CLI posts through `coord.authorize`, rank-strict Intent, board-seeded lease epochs and expiry, reconcile conflict refusal, real supervisor pids with actor-owned timers, fail-closed health with explicit Unknown, opaque scoped expiring grants gating memory, and strict admission (0 Fail ∧ 0 Declared) with a probed NVMe interlock and evaluated checklist. 400 tests, 0 warnings. Admission stays HOLD by design: declared evidence is no longer admissible (journal §3.7, §10).

## Consequences
- Evidence state: `passed`; promotion needs Hermes two-key records and tri-sovereign review.
- Three defects were found by the system's own audits and fixed the same session: ledger lines without delivery records, a secondary screen without the mandated chrome, and a single linear hash chain that a second writer's acknowledgement broke (now per-sender chains).
- Zenoh REST has no subscription push here; sync is reconcile-on-tick. A native subscriber (SSE or the cepaf NIF client) is a follow-on.
