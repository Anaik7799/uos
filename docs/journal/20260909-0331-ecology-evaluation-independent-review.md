# 20260909-0331 — Independent evaluation and paid-dispatch boundary review

#fractal-l0 #fractal-l2 #fractal-l4 #fractal-l5 #fractal-l6 #fractal-l8 #zk-adr #zero-muda #tailscale-web

[UOS](http://nas-1.tail55d152.ts.net:4100/) · [Ecology](http://nas-1.tail55d152.ts.net:4110/ecology) · [Review receipt](http://nas-1.tail55d152.ts.net:4100/files/docs/journal/20260909-0331-ecology-evaluation-independent-review.json) · [Toolchain inventory](http://nas-1.tail55d152.ts.net:4100/files/docs/journal/20260909-0331-ecology-evaluation-toolchains.json)

Sa-plan `uos/ecology-evaluation-review/20260909-0331`, task `EVALREVIEW`, worker `codex-ecology-evaluation-review`, attempt 1. Claimed at 2026-09-09T03:35:07Z. Active observations passed at 03:35:08Z, 03:43:04Z and 03:56:24Z. The latter observed chrony stratum 3, absolute offset 0.000569209 seconds and uncertainty 0.0171316835 seconds. Source files were being implemented concurrently; the receipt records observation hashes, not an atomic admitted release.

## 1. Scope & Trigger

The parent requested an independent review of the proposed ten-domain evaluation suite and discovery of realized native toolchains. The task later included the staged paid-calibration guard, Erlang adapter and Gleam engine. This reviewer owned review artifacts only. Implementation, compiler runs on evaluation fixtures, paid calls, ledger provisioning, release staging and runtime integration remained with the respective owners.

## 2. Pre-State Assessment

Transport success and profile names did not establish coding or decision quality. The proposed catalog was initially unwritten. It now supplies ten small public synthetic cases with strict answer schemas, bounded expression parsing, independent case expectations and fixed native templates. Five domains require a native interpreter; the other five compare structured decisions with OCaml reference semantics. Neither group establishes broad model proficiency.

## 3. Execution Detail

Applied the formal-technique-selection skill as read-only guidance. An initial risk preflight held because that skill path resolves to an external symlink; the risk evidence was repaired to use canonical regular policy files without copying external content or forcing a pass. Created and claimed the single task after the repaired preflight, then refreshed the assessment for the guard review.

Observed version/help output for pinned Gleam, OCaml, Lean, Quint, Mojo and OTP; resolved tool and backend paths and streamed SHA256 over eleven executable/backend files. No evaluation fixture, solver query or application build was executed by this reviewer. Required isolated administrative risk checks are separate. Read the evaluator, native templates, tests, paid guard, adapter, engine, worker, HTTP transport and ledger. Sent concrete findings to their owners while implementation continued.

## 4. Root Cause Analysis

L5/L6 quality failures arise when API availability, syntax, shared interpreter behavior and independent task correctness are merged into one success flag. L4/L8 dispatch failures arise when a task observation, component reservation, provider bill and process lock are treated as equivalent authority. Both problems require typed distinctions and observed falsifiers rather than stronger status labels.

The installed Quint CLI can return success with zero selected tests. The two expression interpreters share primitive operations. The first native trial exposed a missing Mojo standard-library environment and a Lean memory limit. These are harness findings, not model failures; preserving them prevents false rankings.

## 5. Fix Taxonomy

This task adds evidence and recommendations, with no existing source edits. Peer repairs observed in staged source include a nonzero named Quint witness, compiled evaluator/source fingerprint comparison, finite paid-task authorization, read-only Sa-plan queries, immutable request preparation, body equality at reservation, pre-dispatch candidate/module evidence and an honest cooperative-lock scope. The first review snapshot recorded native setup and consumer controls as pending. The final evaluator follow-up below records their subsequent source freeze and observed peer test output.

## 6. Patterns & Anti-Patterns Discovered

**ER-01 — Quality scope.** Call these constrained semantic repairs and decisions. The Lean grammar selects from four fixed tactics; the Rete case checks relational join/retraction semantics, not the full UL network or unlinking performance. A single case per domain cannot rank general programming or safety competence. Preserve per-domain pass@1, format failures, infrastructure failures, all retries, reserved liability, measured cost and latency.

**ER-02 — Independent, nonvacuous checks.** `observe` and `compile` share primitive operations, so their agreement is not an independent correctness oracle. Separate catalog expectations and actual native assertions supply the useful comparison. Require the exact seven Quint test names and count, reject zero tests, and retain wrong-expression/proof, timeout, missing-tool and unknown-result controls. The current source explicitly acknowledges these limits.

**ER-03 — Evidence binding, source and controls repaired.** The evaluator initially recomputed a mutable source hash without comparing it to its compiled bytes. The frozen CLI now compares its source/guardian/builder fingerprint against a build-time constant before any command. Its old compiled executable was refused with exit 2. The final source checks tool SHA256 before and after native execution and reports the transitive-library limit. Initially failed score/native-selftest JSON could accompany exit 0; final consumer controls show wrong answer exit 1, invalid markdown exit 2 and a verified answer exit 0. Callers must still inspect structured status.

**ER-04 — Paid authority and body, P1 findings repaired in source.** The first guard accepted any executing task, opened Sa-plan through a migration-capable store constructor and bound counts/hash without proving the full provider-policy body. The revised guard authorizes only the exact parent task/worker/attempt, uses fixed READONLY/query-only task and dependency queries, verifies an immutable release, prepares the body from its worker, and requires byte equality plus unchanged source hashes before reservation. Compilation and executed candidate evidence are separate obligations.

**ER-05 — Liability and single-flight scope.** The adapter names one canonical ledger and has no caller-supplied alternate path. The engine checks authority before credential/catalog access, again before reservation and again before its one POST. Denied or uncertain grants cannot POST; there is no retry/refund path. A parent-only advisory file lock is cooperative runner serialization and can be released by parent death while a child still runs. This is now disclosed. The atomic ledger remains the reservation boundary; neither it nor this runner controls unrelated clients on the provider account.

**ER-06 — Credential and provider bounds.** The key stays in a transport closure and authorization header, outside request arguments and evidence. The transport fixes the official TLS endpoint and refuses redirects; the runner suppresses crash dumps. No key bytes were read, copied or hashed by this reviewer. Exact provider-price ceilings and token/body limits constrain the prepared request. The extra 2048 prompt-template tokens are an assumption checked against returned usage, not a tokenizer proof. A returned provider violation may already cost money and must retain its full reservation.

## 7. Verification Matrix

| Domain | Independent criterion and falsifier | Scope |
|---|---|---|
| Gleam | Admission truth table and native assertions; constant true must fail | Finite expression repair |
| OCaml | Lease/token/version predicate at expiry and stale boundaries; `<= expires` must fail | Seeded and boundary cases |
| Mojo | Signed clamp with inside/outside/equal bounds; identity must fail | Fixed numeric kernel |
| Lean | Fixed theorem checked by the kernel, only declared `propext`/`Quot.sound`; `rfl`/`sorryAx` must fail | Tactic selection, no general proof claim |
| Quint | Six truth-table tests plus productive scenario; zero tests and false gate must fail | Bounded scenarios, no exhaustive proof |
| STM | Three accepted/two stale writes and exact final state | CAS trace, not live ETS concurrency |
| Bayesian | Verified pass/fail update only; unknown evidence ignored | Exact Beta arithmetic, no quality authority |
| Rete | Independent relation before/after support retraction | Observable join semantics; full UL unsupported |
| STPA | All four UCA classes and no effect authority | Fixed classification exercise |
| FMEA | Preserve canonical S/O/Det 1..5 and products; safety class/readiness before RPN | No model-generated authority |

Peer-reported first native trial: Gleam/OCaml/Quint passed; deliberately wrong Gleam/Lean and zero-test Quint were rejected; Mojo and Lean had setup/resource failures. The peer repaired Mojo with explicit configuration and retained the Lean 256 MiB failure before using a bounded 512 MiB allowance.

**Final evaluator follow-up:** the owner froze suite `8310205c9520d6ed713caca62c2a28bfe99165eaa37df710c2858e8241b9b7c9`. This reviewer read its actual final JSON: 187 pure checks, all five native positive cases and all eight native/consumer controls pass. The wrong Gleam expression reaches a runtime assertion failure; wrong Lean produces `sorryAx` and fails; empty Quint exits 0 but lacks the required witness. The Lean positive reports exactly `[propext, Quot.sound]`, now explicitly allowed by the fixed template policy; this is not an axiom-free proof claim. Tool SHA256 checks cover entrypoint bytes, with transitive libraries explicitly outside that hash scope. The adjacent final-evaluator receipt binds these observations and the stale-binary control. The reviewer did not rerun native cases or call a model.

Engine test source covers initial/stale/post-reservation fence denial, body mismatch, bad timeout, missing/corrupt/exhausted/duplicate injected grants, bad usage and uncertain transport without retry. The actual guard's 13 input-boundary tests and final frozen-engine execution are owned by root. A mocked no-refund ordering test is not itself durable SQLite recovery evidence; pair it with actual receipt-loss/restart/race tests.

## 8. Files Modified

Only this timestamped journal, its adjacent machine receipt, toolchain inventory and administrative risk/check/completion receipts under `docs/journal/` were added. Temporary OCaml writers under `/tmp/` support those artifacts. No existing implementation, service, database, external source, VCS data or package was edited by this reviewer.

## 9. Architectural Observations

One effect owner should bind a public synthetic case, suite fingerprint, exact task, profile/model, body, release and retained reservation. The quality evaluator should separately authenticate and consume each answer receipt once. Model priors and transport success remain separate from verified trials. Budgeted calibration of initially unknown models is a distinct authorized activity; it does not loosen production selection requirements.

The tool inventory provides realized paths and byte hashes, plus unexecuted command templates. The staged evaluator chooses Quint's TypeScript backend; the inventory also documents the realized Rust backend and Apalache installation. No Apalache server was started. The existing Mojo activation script was read only: it includes first-activation writes and telemetry, so its environment values were extracted explicitly rather than executing that script.

## 10. Remaining Gaps

The evaluator's final selftests, stale-executable/consumer controls and entrypoint hashes were inspected in the follow-up. Final compiled paid-engine binding, durable receipt-loss/restart observation and actual calibration results remain owner tasks. This review has no authority to run paid effects or admit the system. Root should bind suite/case source hashes to each request and keep unsuccessful attempts in the cost and quality record. The prior free/MAX browser receipt belongs to its tested candidate and does not transfer to these new sources.

## 11. Metrics Summary

Ten domains reviewed; eleven realized tool/backend byte hashes; six finding groups; six command templates explicitly unexecuted. Reviewer model calls, network requests, implementation/fixture builds, dependency installations, runtime changes, existing-source edits and VCS mutations: zero. Raw review FMEA S4/O3/Det3/RPN36; P1 score 768 is an analyst prioritization, not effect authority.

## 12. STAMP & Constitutional Alignment

The four unsafe control-action classes are omitting necessary falsifiers; granting quality/effect credit from weak evidence; reporting stale or mixed-source results; and continuing outside the review lease/scope. Controls are the canonical claim/check, staged source hashes, independent oracles, explicit no-call authority, early bounded findings and preserved failures.

Seventeen release aspects are scoped as follows: storage/hardware, VCS/provenance, purity, OTP supervision, ZigVM/VFS, Hermes analysis, formal mathematics, authority, MAX/Mojo, Zenoh, AG-UI, UI components, accessibility/UCon, Tailnet, checklist, KM triad and Sa-plan/workflows. This task observes source/tool evidence and writes documents; it does not assert runtime acceptance in any of those aspects. Root groups the journal/wiki/ZK/KM artifacts. The admitted EV ceiling remains 93 and no new EV number is minted.

## 13. Conclusion

The proposed evaluator has a useful bounded scope with strict parsing and meaningful independent controls. The reviewed paid entrypoint now contains the main authority/body repairs, with cooperative-lock and provider-billing limitations disclosed. Final candidate execution and calibration evidence are still required. This concludes the scoped independent review, not a release approval or system admission.

<details><summary>Verification checklist — five domains, 18 checkpoints</summary>

| Domain | Checkpoints | Scoped evidence |
|---|---|---|
| Metadata/navigation | CHK-01-TIME, CHK-02-TAIL, CHK-03-FRACT, CHK-04-KM | Observed UTC, full links, tags, parent KM grouping |
| Purity/storage | CHK-05-MUDA, CHK-06-GRAPH, CHK-07-DRIVE | No packages, source ingestion or device effects |
| Verification | CHK-08-C1C8, CHK-09-MATH, CHK-10-9MOD, CHK-11-REGR | Per-domain oracles and falsifiers; peer execution attributed |
| Runtime/observability | CHK-12-GLEAM, CHK-13-HERMES, CHK-14-ZIGVM, CHK-15-MAX, CHK-16-OTEL | Local source/tool observation; no runtime acceptance |
| Governance/JJ | CHK-17-SOV, CHK-18-JJ | Exact task attempt, no VCS effect or admission |

Provenance is scope-qualified throughout. Unrun, unsupported, staged and peer-reported states are not silently promoted to verified.
</details>

**Previous:** [Router review](http://nas-1.tail55d152.ts.net:4100/docs/journal/20260909-0338-ecology-router-independent-review.md) · **Next:** [Toolchain inventory](http://nas-1.tail55d152.ts.net:4100/files/docs/journal/20260909-0331-ecology-evaluation-toolchains.json)

**UOS footer:** Tailnet links identify canonical navigation; they do not assert that newly written workspace documents have been published by the running site.
