<!-- GENERATED by `ops agents`. Do not edit: edit the dune files or
     the ownership notes in ops_agents.ml and regenerate, or this
     guide and the tree disagree and the guide is the one nobody
     believes. -->

# `modules/hermes_ops` — agent guide

Read `AGENTS.md` at the repository root first; it governs. This file records what is true of **this module** and nothing else.

> The offload layer (R22). Everything here is an **operator tool**: R19 applies in full — refuse unknown flags, write atomically, capture the real exit code, fail closed. Never report a measurement nothing measured.

## Shape

| | |
|---|---:|
| libraries / executables declared | 39 |
| implementation files (`.ml`) | 69 |
| interfaces (`.mli`) | 38 |
| test executables | 29 |
| dune files | 1 |

**Declared:** `hermes_debugging`, `hermes_external_access`, `hermes_ops`, `hermes_ops_capability`, `hermes_ops_capability_core`, `hermes_ops_completion_topology`, `hermes_ops_config_authority`, `hermes_ops_governance`, `hermes_ops_topology`, `ops_main`, `test_debug_system`, `test_external_access_census`, `test_external_access_formal`, `test_external_access_fractal`, `test_external_access_observability`, `test_external_access_otp_register`, `test_external_access_rete`, `test_external_access_runtime`, `test_external_access_smt`, `test_external_access_stan`, `test_module_intent`, `test_ops_capability`, `test_ops_capability_gate`, `test_ops_command`, `test_ops_command_runtime`, `test_ops_command_service`, `test_ops_completion_history`, `test_ops_config`, `test_ops_config_fractal`, `test_ops_formal_staging`, `test_ops_governance`, `test_ops_governance_formal`, `test_ops_governance_model`, `test_ops_mbse`, `test_ops_mcp`, `test_ops_mutate`, `test_ops_verification_target`, `test_ops_verify`, `test_ops_zenoh`

**Suites:** `test_debug_system`, `test_external_access_census`, `test_external_access_formal`, `test_external_access_fractal`, `test_external_access_observability`, `test_external_access_otp_register`, `test_external_access_rete`, `test_external_access_runtime`, `test_external_access_smt`, `test_external_access_stan`, `test_module_intent`, `test_ops_capability`, `test_ops_capability_gate`, `test_ops_command`, `test_ops_command_runtime`, `test_ops_command_service`, `test_ops_completion_history`, `test_ops_config`, `test_ops_config_fractal`, `test_ops_formal_staging`, `test_ops_governance`, `test_ops_governance_formal`, `test_ops_governance_model`, `test_ops_mbse`, `test_ops_mcp`, `test_ops_mutate`, `test_ops_verification_target`, `test_ops_verify`, `test_ops_zenoh`

## Depends on

- `digestif`
- `hermes_debugging`
- `hermes_external_access`
- `hermes_fpp_window_authority`
- `hermes_harness_suite_telemetry`
- `hermes_ops`
- `hermes_ops_capability`
- `hermes_ops_capability_core`
- `hermes_ops_config_authority`
- `hermes_ops_dashboard`
- `hermes_ops_governance`
- `hermes_wiki_fpp`
- `hermes_wiki_topology`
- `re_export`
- `smtml`
- `unix`
- `yojson`

A dependency added to a dune file appears here on the next regeneration. If it does not, the guide was not regenerated — that is the only way these two can disagree.

**The dependency unit is the LIBRARY, not this directory.** A module directory may declare libraries at several depths, so two directories can each depend on the other while the library graph stays a DAG — dune proves that, since it rejects a library cycle at build time and the workspace builds. `modules/hermes_ops` and `modules/hermes_ops_dashboard` are the worked example: the aggregate `hermes_ops` depends on the dashboard, while the dashboard depends on the leaves `hermes_ops_capability`, `hermes_ops_governance` and `hermes_ops_topology`, which the aggregate re-exports so consumers keep the same unwrapped module names. Read the list above as libraries; a directory-level reading of it reports cycles that do not exist.

## What binds work here

- **Interfaces first.** Write the `.mli` with each law in prose — what is true and *why it must be* — then the `.ml`. Never ship an `.mli` stating a law the `.ml` does not enforce.
- **Total at the edges.** Malformed input clamps to a defined value or returns a named error; it never raises and never silently drops an author's text.
- **A skip is disclosed and counted** (R2). Missing evidence is unavailable, never passing.
- **Verify with the offload layer**, not a loop: `dune exec modules/hermes_ops/ops_main.exe -- verify` (R22).
- **Mutation-test with `ops mutate`.** A survivor is a finding, not a failure; a void run is not a result.

## Verify

```
eval $(opam env --switch=/home/an/dev/ver/zigvm --set-switch)
dune build
dune exec modules/hermes_ops/ops_main.exe -- verify
```
