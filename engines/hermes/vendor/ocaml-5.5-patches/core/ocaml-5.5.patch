From d1bd40b5c4d05f71d998b0b722579ae5548b692f Mon Sep 17 00:00:00 2001
From: Your Name <your.email@example.com>
Date: Sun, 2 Aug 2026 23:13:04 +0200
Subject: [PATCH 1/3] compat: model OCaml 5.5 GC live stack words

---
 core/src/gc.ml | 3 +++
 1 file changed, 3 insertions(+)

diff --git a/core/src/gc.ml b/core/src/gc.ml
index 323c8a9..6349dcb 100644
--- a/core/src/gc.ml
+++ b/core/src/gc.ml
@@ -68,6 +68,7 @@ module Stable = struct
         ; top_heap_words : int
         ; stack_size : int
         ; forced_major_collections : int
+        ; live_stacks_words : int
         }
       [@@deriving
         bin_io
@@ -125,6 +126,7 @@ module Stat = struct
       ; top_heap_words : int
       ; stack_size : int
       ; forced_major_collections : int
+      ; live_stacks_words : int
       }
     [@@deriving
       compare ~localize
@@ -161,6 +163,7 @@ module Stat = struct
     ; stack_size = int_f first.stack_size second.stack_size
     ; forced_major_collections =
         int_f first.forced_major_collections second.forced_major_collections
+    ; live_stacks_words = int_f first.live_stacks_words second.live_stacks_words
     }
   ;;
 
-- 
2.51.2


From 6960ea7c7ace5138d7af262e8dbba5d8e1152764 Mon Sep 17 00:00:00 2001
From: Your Name <your.email@example.com>
Date: Sun, 2 Aug 2026 23:16:30 +0200
Subject: [PATCH 2/3] compat: expose OCaml 5.5 live stack words

---
 core/src/gc.mli | 2 ++
 1 file changed, 2 insertions(+)

diff --git a/core/src/gc.mli b/core/src/gc.mli
index 8ebbbc3..6fae738 100644
--- a/core/src/gc.mli
+++ b/core/src/gc.mli
@@ -102,6 +102,8 @@ module Stat : sig
     ; forced_major_collections : int
     (** Number of forced full major collection cycles completed since the program was
         started. *)
+    ; live_stacks_words : int
+    (** Number of words occupied by live fiber stacks. *)
     }
   [@@deriving
     sexp_of
-- 
2.51.2


From af07077cbe7d7d8c6d44355e58f85853aaa5dead Mon Sep 17 00:00:00 2001
From: Your Name <your.email@example.com>
Date: Sun, 2 Aug 2026 23:30:50 +0200
Subject: [PATCH 3/3] compat: bind command map existential on OCaml 5.5

---
 command/src/command.ml | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/command/src/command.ml b/command/src/command.ml
index bd4043e..449c3aa 100644
--- a/command/src/command.ml
+++ b/command/src/command.ml
@@ -249,13 +249,14 @@ end = struct
       let make_map_raise_duplicate_key
         (type cmp)
         (module S : S with type comparator_witness = cmp)
-        alist
+        (alist : (string * a) list)
+        : (string, a, cmp) Map.t
         =
         match Map.of_alist (module S) alist with
         | `Ok map -> map
         | `Duplicate_key (_ : S.t) ->
           let duplicate_keys =
-            List.map alist ~f:(fun (k, (_ : 'a)) -> k, k)
+            List.map alist ~f:(fun (k, (_ : a)) -> k, k)
             |> Map.of_alist_multi (module S)
             |> Map.filter ~f:(function
               | [] | [ _ ] -> false
-- 
2.51.2

