# Native OCAML Bounded Kernels (`native/nifs/ocaml/`)
- **Authority**: Bounded, deterministic computational kernel.
- **Invariants**: Zero blocking calls; failsafe timeouts; bounded memory envelopes.
