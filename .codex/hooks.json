{
  "description": "UOS session-start policy reminder; advisory only, with no enforcement or admission claim.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "test -d '/home/an/NAS-setup/uos/.jj' && printf '%s\\n' '{\"hookSpecificOutput\":{\"hookEventName\":\"SessionStart\",\"additionalContext\":\"UOS standalone Jujutsu workspace. Follow AGENTS.md for Zero-Muda, Gleam/OTP supervision, Sa-plan authority, and current verification gates. This hook is an advisory reminder; it does not enforce policy or establish runtime admission.\"}}'",
            "timeout": 5
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd /home/an/NAS-setup/uos && out=$(bash tools/preflight --max-age 900 --quiet 2>&1) || out=$(bash tools/preflight --receipt --quiet 2>&1 | tail -1); printf '{\"hookSpecificOutput\":{\"hookEventName\":\"SessionStart\",\"additionalContext\":\"UOS toolchain preflight: %s. Advisory: a passing preflight is evidence the toolchain works, not admission authority.\"}}' \"$(printf '%s' \"$out\" | tr -d '\\\"' | tr '\\n' ' ' | cut -c1-300)\"",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
