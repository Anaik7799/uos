---
name: tensor-space-evolution
description: "Executes 10-dimensional tensor evolutionary cycles across Web, Wiki, ZK, and KM: V_13 x S x (L_0..L_9) x SDL x SRE x CX x DX x UX x Navigation x Utility with tri-sovereign governance (Gemini -> Codex Astra -> Claude Fable 5.1)."
---

# Tensor Space Evolution Skill (SC-TENSOR-EVOLVE-001)

## 1. Overview & Mandate
This skill governs the evolution and formal verification of the Unified Operational System (UOS) across the full 10-dimensional tensor space:
$$\mathbf{TensorSpace} = \vec{\mathcal{V}}_{13} \times \mathcal{S} \times (L_0 \dots L_9) \times \text{SDL} \times \text{SRE} \times \text{CX} \times \text{DX} \times \text{UX} \times \text{Navigation} \times \text{Utility}$$

Every feature, enhancement, and page must be validated through the tri-sovereign pipeline:
1. **Gemini**: Pure Gleam Lustre implementation, unit/property tests, zero-muda verification.
2. **Codex Astra**: Formal and algorithmic audit across all 10 tensor dimensions.
3. **Claude Fable 5.1**: STAMP/STPA safety analysis and constitutional ratification certificate.

## 2. 10-Dimensional Tensor Mapping
1. **V13 Traceability**: Lean 4 coordinate conservation law $\Delta \vec{\mathcal{T}}_{13} \equiv \mathbf{0}$, fail-closed trust indicator $\mathbb{I}(\text{Trust}) = \mathbb{I}(\text{Runtime}) \times \mathbb{I}(\text{Spec})$.
2. **Fractal Surfaces ($S$)**: 5 synchronized surfaces (LustreWeb :4100, WispApi :4100, AnsiTui CLI, AgUiSse `/ag-ui/events`, MozZenoh pub/sub).
3. **Fractal Layers ($L_0 \dots L_9$)**: Complete hierarchy from $L_0$ Constitutional to $L_9$ Trans-Knowledge.
4. **SDL (Software Development Lifecycle)**: Gospel formal contracts, bounded Z3 SMT solver queries ($\le 2000\text{ms}$), Rete-UL forward-chaining rules.
5. **SRE (Site Reliability Engineering)**: Prajna 3-state circuit breakers (`Closed | HalfOpen | Open`), Lyapunov exponential stability ($\lambda \le -0.05$), dead-man freshness monitors.
6. **CX (Customer Experience)**: Instant Tailscale FQDN reachability (`http://nas-1.tail55d152.ts.net:4100/`), mean transit latency $\le 15\text{ms}$, transparent live indicators.
7. **DX (Developer Experience)**: Zero compiler warnings (`SC-MUDA-001`), sub-second BEAM compilation loop, standalone Jujutsu monorepo (`.jj/`).
8. **UX (User Experience)**: WCAG 2.1 Level AAA contrast ratio ($\ge 7:1$), Core Web Vitals ($LCP \le 2.5\text{s}$, $INP \le 200\text{ms}$, $CLS \le 0.1$), keyboard shortcuts (`Cmd+K`), dual source mode.
9. **Navigation Sheaf**: Uniform grouped sidebar, hierarchical breadcrumbs, linear prev/next, Dung abstract argumentation framework defending 18 unattacked checklist invariants.
10. **Utility & Safety Interlock**: Rocha biosemiotics symbol-matter cut decoupling syntax from execution, host root OS NVMe hard lock (`HARD_DENIED_SYSTEM_OS_SERIAL = "25503L801736"`).

## 3. Execution Standard Operating Procedure
1. Create dedicated Gleam Lustre module in `apps/cepaf_gleam/src/cepaf_gleam/ui/lustre/`.
2. Author comprehensive unit/property tests in `apps/cepaf_gleam/test/`.
3. Mount the route in `apps/indrajaal_gleam_web/src/indrajaal_gleam_web.gleam` and add sidebar navigation links.
4. Verify HTTP 200 OK via `curl -I http://127.0.0.1:4100/<route>`.
5. Dispatch audit request to Codex Astra subagent.
6. Dispatch safety & constitutional review to Claude Fable 5.1 subagent.
7. Update SQLite persistence database (`uos_verification_tracking.sqlite3`) and TOML inventory.
8. Commit under standalone Jujutsu (`jj describe` and `jj new`).

## 4. Reusable Master Prompt Specification
For multi-cycle evolutionary execution, consult the canonical parameterized specification:
- `docs/design/20260906-0835-uos-tri-sovereign-10d-tensor-evolution-reusable-master-prompt.md` (`SPEC-20260906-0835-REUSABLE-TENSOR-MASTER-PROMPT`)
- Tailscale Web Link: [http://nas-1.tail55d152.ts.net:4100/docs/design/20260906-0835-uos-tri-sovereign-10d-tensor-evolution-reusable-master-prompt.md](http://nas-1.tail55d152.ts.net:4100/docs/design/20260906-0835-uos-tri-sovereign-10d-tensor-evolution-reusable-master-prompt.md)
