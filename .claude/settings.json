{
  "uos_control_loop": {
    "PreInvocation": [
      {
        "command": "test -d /home/an/NAS-setup/uos/.jj && echo \"{\\\"systemMessage\\\":\\\"UOS Standalone Jujutsu repository active. Zero-Muda and OTP29 rules enforced.\\\"}\"",
        "timeout": 5,
        "type": "command"
      },
      {
        "command": "cd /home/an/NAS-setup/uos && out=$(bash tools/preflight --max-age 900 --quiet 2>&1) || out=$(bash tools/preflight --receipt --quiet 2>&1 | tail -1); printf '{\"systemMessage\":\"UOS toolchain preflight: %s\"}' \"$(printf '%s' \"$out\" | tr -d '\\\"' | tr '\\n' ' ' | cut -c1-300)\"",
        "timeout": 120,
        "type": "command"
      }
    ]
  }
}
