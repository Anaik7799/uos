#!/usr/bin/env bash
# In-project toolchain wrapper (SC-TOOLCHAIN-INPROJECT-001). No $HOME paths.
set -euo pipefail
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/lib/uos-toolchain.sh"
exec "$(uos_tool lean)" "$@"
