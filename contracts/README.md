# Contracts Subsystem (`contracts/`)

- **Ownership & Language Authority**: Schema specifications (Cap'n Proto, Protobuf, JSON-Schema).
- **Scope**: Cross-boundary schemas, API contracts, inter-service messaging definitions, and serializable trace packets.
- **Invariants**: Explicit schema versioning and backward compatibility enforcement.
